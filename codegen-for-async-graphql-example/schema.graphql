# schema {
#   query: Root
# }

type Root {
  me: Me!
  active: Bool!
}

type FriendConnection {
  totalCount: Int!
}

type Me {
  id: ID!
  name: String!
  rank: Float!
  email: String
  age: Int
  active: Bool
  friends: FriendConnection!
}
